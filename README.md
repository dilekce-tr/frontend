# YazbirDilekçe — Frontend

Nuxt 4 + Vue 3. See `../CLAUDE.md` for the full product brief.

```bash
npm install
npm run dev      # http://localhost:3000
npm run build
```

The dev server expects the Rails backend on `http://localhost:3012`. Override with `NUXT_PUBLIC_API_BASE`.
